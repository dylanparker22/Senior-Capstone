name: Deploy React App to Azure Static Web App

on:
  push:
    branches:
      - main  # Change if using a different branch

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18  # Change to match your project

      - name: Install dependencies
        run: |
          cd client/fetch-api-app
          npm install  # Use install since package-lock.json is missing

      - name: Build React app
        run: |
          cd client/fetch-api-app
          npm run build  # Creates production build

#      - name: Build And Deploy to Azure
#        id: builddeploy
#        uses: Azure/static-web-apps-deploy@v1
#        with:
#          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_VICTORIOUS_HILL_058CD2610 }}
#          action: "upload"
#          ###### Repository/Build Configurations - These values can be configured to match your app requirements. ######
#          # For more information regarding Static Web App workflow configurations, please visit: https://aka.ms/swaworkflowconfig
#          app_location: "./client/fetch-api-app" # App source code path
#          api_location: "" # Api source code path - optional
#          output_location: "build" # Built app content directory - optional
 #         github_id_token: ${{ steps.idtoken.outputs.result }}
 #         ###### End of Repository/Build Configurations ######

  close_pull_request_job:
    if: github.event_name == 'pull_request' && github.event.action == 'closed'
    runs-on: ubuntu-latest
    name: Close Pull Request Job
    steps:
      - name: Close Pull Request
        id: closepullrequest
        uses: Azure/static-web-apps-deploy@v1
        with:
          action: "close"

